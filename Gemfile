# Local preview only. GitHub Pages ignores this file and builds with its own
# Jekyll 3.10; the page uses nothing that differs between that and Jekyll 4.
#
#   bundle install
#   bundle exec jekyll serve      ->  http://localhost:4000
#
# (The github-pages gem itself would match exactly, but it needs Ruby < 4.0.)
source "https://rubygems.org"

gem "jekyll", "~> 4.4"

# Windows has no zoneinfo database of its own.
gem "tzinfo-data", platforms: [:windows]
